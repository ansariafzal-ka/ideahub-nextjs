import Form from "@/components/Form";

const CreatePage = () => {
  return (
    <section className="w-full px-6 mt-6">
      <Form type="Create" />
    </section>
  );
};

export default CreatePage;
